<!doctype html>
<html
  {% if request.design_mode %}
    class="shopify-design-mode"
  {% endif %}
  lang="{{ request.locale.iso_code }}"
>
  <head>
    {%- if settings.favicon != blank -%}
      <link
        rel="icon"
        type="image/png"
        href="{{ settings.favicon | image_url: width: 32, height: 32 }}"
      >
    {%- endif -%}

    {% # theme-check-disable RemoteAsset %}
    {% comment %} This a way to wait for main content to load when navigating to a new page so that the view transitions can work consistently {% endcomment %}
    {% if settings.transition_to_main_product or settings.page_transition_enabled %}
      <link
        rel="expect"
        href="#MainContent"
        blocking="render"
        id="view-transition-render-blocker"
      >
    {% endif %}

    {%- render 'meta-tags' -%}
    {%- render 'stylesheets' -%}
    {%- render 'fonts' -%}
    {%- render 'scripts' -%}
    {%- render 'theme-styles-variables' -%}
    {%- render 'color-schemes' -%}

    {% if request.design_mode %}
      {%- render 'theme-editor' -%}
    {% endif %}

    {{ content_for_header }}
    {% # theme-check-enable RemoteAsset %}
    <style>
      /* Global Background Image (Fallback/Inner pages) */
      body {
        background-color: #040d0a !important;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)' opacity='0.06'/%3E%3C/svg%3E") !important;
        background-repeat: repeat !important;
        background-attachment: fixed !important;
      }
      /* Custom Background Image for Homepage */
      body.template-index {
        background-image: url("{{ 'Backgroundimage.png' | asset_url }}") !important;
        background-size: cover !important;
        background-position: center top !important;
        background-repeat: no-repeat !important;
        background-attachment: fixed !important;
      }
      /* Force Custom Image Logo and Remove Text Logo Fallbacks */
      .logo-section__image-wrapper, .logo-section__image, .header-logo img, .logo-monogram, .logo-inline img {
        display: none !important; 
      }
      .header-logo, .logo-title {
        font-size: 0 !important;
        color: transparent !important;
      }
      .logo-section jumbo-text, .logo-block jumbo-text {
        display: none !important;
      }
      .logo-title, .logo-section::after, .header-logo::after, .logo-inline::after {
        content: "";
        display: inline-block;
        background-image: url("{{ 'visriva-logo.png' | asset_url }}");
        background-size: contain;
        background-repeat: no-repeat;
        background-position: center;
        width: 180px;
        height: 50px;
      }
      .header__column--left .header-logo::after {
        background-position: center left;
      }
      .logo-section::after {
        width: 250px;
        height: 80px;
      }
      /* Homepage Transparency & Glassmorphism for Global Background Visibility */
      body.template-index,
      body.template-index #MainContent,
      body.template-index .section,
      body.template-index .section-background,
      body.template-index .color-scheme-1,
      body.template-index .color-scheme-2,
      body.template-index .color-scheme-3,
      body.template-index .color-scheme-4,
      body.template-index .color-scheme-5,
      body.template-index .color-scheme-6,
      body.template-index .color-scheme-7,
      body.template-index .color-scheme-8,
      body.template-index [class*="color-scheme-"],
      body.template-index #header-group,
      body.template-index header-component,
      body.template-index footer,
      body.template-index .footer-section,
      body.template-index .layered-slide, 
      body.template-index .slide, 
      body.template-index .carousel-content,
      body.template-index .marquee {
        background-color: transparent !important;
        background: transparent !important;
      }
      body.template-index .card,
      body.template-index .product-card,
      body.template-index .collection-card,
      body.template-index .blog-post-card {
        background: rgba(4, 13, 10, 0.45) !important;
        backdrop-filter: blur(12px) !important;
        -webkit-backdrop-filter: blur(12px) !important;
        border: 1px solid rgba(191, 149, 63, 0.25) !important;
        border-radius: 12px !important;
        padding: 15px !important;
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3) !important;
      }
      body.template-index .card__image,
      body.template-index .product-card__image,
      body.template-index .collection-card__image,
      body.template-index .image-element img {
        background-color: transparent !important;
      }
    </style>
    <link href="{{ 'visriva-global.css' | asset_url }}" rel="stylesheet">
  </head>

  <body class="template-{{ template.name }} page-width-{{ settings.page_width }} card-hover-effect-{{ settings.card_hover_effect }}">
    {% render 'skip-to-content-link', href: '#MainContent', text: 'accessibility.skip_to_text' %}
    <div id="header-group">
      {% sections 'header-group' %}
    </div>

    <script>
      // Inline header height and menu style calculations to prevent layout shift
      // Note: Updates in calculateHeaderGroupHeight(), updateTransparentHeaderOffset() and setHeaderMenuStyle() utilities.js should be kept in sync with this function
      (function setHeaderHeighCustomProperties() {
        /*
         * Header calculation functions for maintaining CSS variables
         * Mimic calculateHeaderGroupHeight() in utilities.js
         */
        const header = document.querySelector('header-component');
        const headerGroup = document.querySelector('#header-group');
        const hasHeaderSection = headerGroup?.querySelector('.header-section');

        if (!header || !headerGroup) return;

        const headerTopRow = header.querySelector('.header__row--top');
        const headerHeight = header.offsetHeight;

        // Calculate the total height of the header group
        let headerGroupHeight = 0;
        const children = headerGroup.children;
        for (let i = 0; i < children.length; i++) {
          const element = children[i];
          if (element === header || !(element instanceof HTMLElement)) continue;
          headerGroupHeight += element.offsetHeight;
        }

        // Check for transparent header special case
        if (header.hasAttribute('transparent') && header.parentElement?.nextElementSibling) {
          headerGroupHeight += headerHeight;
        }

        // Set CSS variables
        document.body.style.setProperty('--header-height', `${headerHeight}px`);
        document.body.style.setProperty('--header-group-height', `${headerGroupHeight}px`);

        if (headerTopRow) {
          window.requestAnimationFrame(function () {
            document.body.style.setProperty('--top-row-height', `${headerTopRow.offsetHeight}px`);
          });
        }

        /**
         * Updates CSS custom properties for transparent header offset calculation
         * Mimic updateTransparentHeaderOffset() in utilities.js
         */

        if (!hasHeaderSection || !header?.hasAttribute('transparent')) {
          document.body.style.setProperty('--transparent-header-offset-boolean', '0');
          return;
        }

        const hasImmediateSection = hasHeaderSection.nextElementSibling?.classList.contains('shopify-section');

        const shouldApplyOffset = !hasImmediateSection ? '1' : '0';
        document.body.style.setProperty('--transparent-header-offset-boolean', shouldApplyOffset);
      })();

      (function setHeaderMenuStyle() {
        const headerComponent = document.querySelector('#header-component');
        if (headerComponent) {
          const isTouchDevice = 'ontouchstart' in window && navigator.maxTouchPoints > 0;
          const overflowList = headerComponent?.querySelector('overflow-list');
          const hasReachedMinimum = overflowList && overflowList.hasAttribute('minimum-reached');
          headerComponent.dataset.menuStyle = isTouchDevice || hasReachedMinimum ? 'drawer' : 'menu';
        }
      })();
    </script>

    <main
      id="MainContent"
      class="content-for-layout"
      role="main"
      data-page-transition-enabled="{{ settings.page_transition_enabled }}"
      data-product-transition="{{ settings.transition_to_main_product }}"
      data-template="{{ template }}"
    >
      {{ content_for_layout }}
    </main>

    <footer>
      {% sections 'footer-group' %}
    </footer>

    {% render 'search-modal' %}

    {% if settings.quick_add or settings.mobile_quick_add %}
      {% render 'quick-add-modal' %}
    {% endif %}

    {% if template.name == 'index' %}
      <a href="/pages/superdesign" class="visriva-fab">
        <span class="visriva-fab-icon">✨</span>
        <span class="visriva-fab-text">Design Custom T-Shirt</span>
      </a>
    {% endif %}
  </body>
</html>
